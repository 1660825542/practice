object helloScala {
  def main(args: Array[String]): Unit = {
    var a=new scala.util.Random();
print(a.nextInt(10))
    val n=10;
    val a1=new Array[Int](n);

    for (i<- a1) println(i);
  }
}
